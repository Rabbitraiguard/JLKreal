# JLK Transservice - Simplified Web Server
# Required dependencies for the web application (using Formspree for form handling)

# Core web framework
Flask==2.3.3
Werkzeug==2.3.7

# CORS support for API
Flask-CORS==4.0.0

# Template engine (included with Flask but specified for clarity)
Jinja2==3.1.2

# Production web server
gunicorn==21.2.0

# Note: The following are built-in Python modules and don't need installation:
# - json (JSON handling) 
# - datetime (date/time handling)
# - logging (logging system)
# - os (environment variables)